import { lalezar } from '@/app/ui/fonts';

export default async function Page() {
    return (
      <div className="w-full">
        <div className="flex w-full items-center justify-between pb-4">
          <h1 className={`${lalezar.className} text-2xl`}>مشتریان</h1>
        </div>
        </div>
    )
}